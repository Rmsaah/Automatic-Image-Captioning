# Python
__pycache__/
*.py[cod]
.venv/
venv/

# IDE
.idea/

# Datasets — downloaded separately, see README
flickr8k/
flickr30k/

# Per-experiment checkpoints, ~1.1 GB in total. Only the exported best model is
# tracked; the rest are reproducible by re-running Stage 12.
models/*.pth
!models/best_image_captioning_model.pth

# Superseded root-level checkpoint from before the training fix
/best_image_captioning_model.pth
